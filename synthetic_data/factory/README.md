# Synthetic Factory Data

The seeded factory is Northstar Precision Works, a fictional synthetic manufacturing environment.

The canonical seed lives in `backend/forge/simulator/seed.py` so tests and demo resets use the same source.
