# Synthetic Knowledge Corpus

The knowledge corpus is generated by `build_knowledge_documents()` in `backend/forge/simulator/seed.py`.

It includes troubleshooting procedures, maintenance SOPs, historical incidents, equipment guides, production policies, governance policies, and `MAL-REDTEAM-001`, the deliberate prompt-injection test document.
